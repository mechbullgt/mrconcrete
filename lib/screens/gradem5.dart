import 'package:flutter/material.dart';

class Gradem5 extends StatelessWidget {
 static const String routeName = "/5";

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
        appBar: AppBar(
          title: Text("Grade M5"),
        ),
        body: Text("Grade M5 Rocks"),
      )
    ;
  }
}